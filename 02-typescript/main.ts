var minhavar = 'minha variavel';

function minhaFunc(x, y) {
    return x + y;
}

let num = 5;
const PI = 3.14;

var numeros = [1, 2, 3];

numeros.map(valor => valor * 2);

class Matematica {
    soma(x, y) {
        return x + y;
    }
}

var n: string = "Higor";
n = "Higor Roberto";